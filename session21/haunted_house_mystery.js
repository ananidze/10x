const kitchenHasGhost = true;
const livingRoomHasGhost = true;

if (kitchenHasGhost && livingRoomHasGhost) {
    console.warn('Danger! The house is NOT safe. Ghosts detected!')
} else {
    console.log('The house is safe. No ghosts in the Kitchen or Living Room')
}